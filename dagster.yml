# dagster_home/dagster.yaml

storage:
  run_storage:
    module: dagster._core.storage.runs
    class: SqliteRunStorage
    config:
      base_dir: storage/run_storage

  event_log_storage:
    module: dagster._core.storage.event_log
    class: SqliteEventLogStorage
    config:
      base_dir: storage/event_log

  schedule_storage:
    module: dagster._core.storage.schedules
    class: SqliteScheduleStorage
    config:
      base_dir: storage/schedule_storage

  artifact_storage:
    module: dagster._core.storage.root
    class: LocalArtifactStorage
    config:
      base_dir: storage/local_artifact_storage

local_artifact_storage:
  base_dir: storage/local_artifact_storage

compute_logs:
  module: dagster._core.storage.compute_log_manager
  class: LocalComputeLogManager
  config:
    base_dir: logs/compute_logs

repository:
  module: glucose_ingest_project.definitions
